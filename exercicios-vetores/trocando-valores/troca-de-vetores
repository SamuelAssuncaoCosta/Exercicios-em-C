#include <stdio.h>
int main(){
	int va[8], vb[8],i,j,aux;
	
	for(i=0; i<8;i++){
		printf("insira os valores do vetor A: ");
		scanf("%d", &va[i]);
	}
	
	for(i=0; i<8;i++){
		printf("insira os valores do vetor b: ");
		scanf("%d", &vb[i]);
	}
	
	for(i=0;i<8;i++){
		aux = va[i];
		va[i] = vb[i];
		vb[i] = aux;
	}
	printf("\n--- Vetor A apos a troca ---\n");
    for(i = 0; i < 8; i++) {
        printf("%d ", va[i]);
    }

    printf("\n\n--- Vetor B apos a troca ---\n");
    for(i = 0; i < 8; i++) {
        printf("%d ", vb[i]);
    }


return 0;
}


