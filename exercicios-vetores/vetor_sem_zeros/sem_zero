#include <stdio.h>
int main(){
	int v[30],v2[30], i;
	
	for(i=0 ; i < 30; i++){
		printf("insira o numero: ");
		scanf("%d", &v[i]);
	}
	for(i=0; i < 30; i++){
        if(v[i] == 0){
            v2[i] = 1;  
        } else {
            v2[i] = v[i]; 
        }
    }
    printf("\nVetor Original:\n");
    for(i=0; i < 30; i++){
        printf("%d ", v[i]);
    }

 
    printf("\n\nVetor Modificado (sem zeros):\n");
    for(i=0; i < 30; i++){
        printf("%d ", v2[i]);
    }
    

return 0;
}


